# Dépendances — toutes disponibles dans Python stdlib sauf Flask
flask
# PyMuPDF : optionnel, active la projection de patrons PDF
PyMuPDF
# Pillow : optionnel, optimise les images téléchargées (lossless)
Pillow
